# Vendored from grok-agent main; migrated v2.15 -> v2.14 + extensions.
version: '2.14'
name: trend-aligned-poster
description: Local-first trend-aligned poster for X creators — reads creator-supplied
  trend data (or seeded demo trends) and emits a 7/8-section structured plan with
  4 official Trend Alignment Plan Score metrics (Trend match strength / Niche fit
  / Voice fidelity / Originality), trend-chasing paradox detection in both Trend Alignment
  Plan Score and Red Flags sections, 3–5 trend watchlist entries, 4–6 post ideas (mix
  of single + thread formats) with per-idea trend match score and predicted engagement
  band,...
runtime:
  engine: grok
  model: grok-4.3
deploy:
  targets:
  - worker
category: creator-template
cost_limits:
  usd_per_session_max: 0.3
  usd_per_day_max: 1.0
  tokens_per_session_max: 60000
  api_calls_per_session_max: 80
tools:
- name: generate_trend_aligned_post_plan
  type: local_function
  description: Produce a 7-section structured trend-aligned post plan (or 8-section
    when window=7d, off-niche guard fires, or all trends are >24h old) with 4 official
    Trend Alignment Plan Score metrics, weighted Plan Score, trend-chasing paradox
    detection in BOTH the Plan Score section AND the Red Flags section, 3–5 trend
    watchlist entries, 4–6 post ideas (mix of single + thread formats — off-niche
    trend-chasers excluded and consolidated as a Red Flag), per-idea trend match score
    + predicted engagement band, and mandatory bridges to content-idea-generator +
    thread-builder in every output. Never auto-publishes. Refuses financial, cashtag,
    investment, and sponsorship content.
  module: trend_aligned_poster.run
  function: generate
extensions:
  xlos_runtime:
    type: python_module
    module: trend_aligned_poster
