# spec v2.14

The canonical schema is [`schema.json`](schema.json) (JSON Schema 2020-12). Extension blocks are documented in [`extensions/README.md`](extensions/README.md). Validate manifests via `grok-install validate <path>`.
