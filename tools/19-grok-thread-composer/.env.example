# Copy this file to .env (which is gitignored) and fill in your real key.
# Never commit the real .env. The key is read by the Node proxy ONLY —
# never bundled into the browser. See ../../docs/tokens.md for setup.

XAI_API_KEY=your-key-here
